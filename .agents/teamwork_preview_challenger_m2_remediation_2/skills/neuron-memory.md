# neuron-memory

Manage agent session context by loading learnings, recording history, and pruning obsolete entries from the memory store.

## 1. Beginning of Run (Context Loading)
Run `neuron learn query "<search query>"` to search learnings.

## 2. End of Run (Memory Recording)
Run `neuron history add` and `neuron learn add`.
